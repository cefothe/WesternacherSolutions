spring:
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MySQL5Dialect
      ddl-auto: create
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://database:3306/myDb?createDatabaseIfNotExist=true
    username: root
    password: admin
    testWhileIdle: true
    validationQuery: SELECT 1